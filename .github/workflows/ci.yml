name: CI
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  lint:
    uses: ./.github/workflows/lint.yml
    permissions:
      contents: read
      packages: read
      statuses: write

  test:
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read

  check-dist:
    uses: ./.github/workflows/check-dist.yml
    permissions:
      contents: write
